import { BaseUrl } from "./BaseUrl";


//Post

export const SignUpAdmin = `${BaseUrl}/auth/signup`;
export const SignInAdmin = `${BaseUrl}/auth/login`;

//Get

export const GetCurrentUser = `${BaseUrl}/auth/get-current-user`;

